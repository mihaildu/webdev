/**
 * just testing if __tests__ works
 *
 * this is a different suite than ../refs.test.js
 * files here don't need the *.test.js/*.spec.js name
 */
test.only("boring test", () => {
    expect(true).toBe(true);
});
